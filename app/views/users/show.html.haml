- title("#{@user.username}'s Wish List")
- content_for(:header) do
  = link_to 'Add Wish', new_user_wish_path(@user)
%ul
  - @user.wishes.each do |wish|
    %li[wish]= link_to wish.title, user_wish_path(@user, wish)
